<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="matsuo.rin.zinro.alpha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity" />
        <activity android:name=".PlayerRegisterActivity" />
        <activity android:name=".FirstNightActivity" />
        <activity android:name=".PositionsSettingsActivity" />
        <activity android:name=".NoonActivity" />
        <activity android:name=".NoonJudgementActivity" />
        <activity android:name=".NightActivity" />
        <activity android:name=".NightKillResultActivity" />
        <activity android:name=".ResultActivity"></activity>
    </application>

</manifest>